


				Текущий проект построен на Spring boot (без использования MVC).


Суффикс -mvc в названии проекта был добавлен на стадии формирования проекта, когда ещё не было понятно, что MVC в проекте не будет.

У приложения есть свой томкот, поэтому установка производится по зелёной стрелочке.

На базе этого проекта сделаны ДЗ-3, ДЗ-4 и ДЗ-5.


Проверить:
	* может ли setId() быть private ?
	-- может.

	* должен ли ProductDao быть компонентом ?
	-- да, т.к. он внедряется в компонент ProductRepository.

	* переделать логику создания нового продукта
	-- сделано.

	* можно ли уже к ProductRepository прикрепить @Repository ?
	-- работает нормально с аннотацией @Repository при использовании MySQL. С аннотацией @Component тоже работает нормально в отсутствие исключений.


	* будет ли работать H2 при включенном MySQL Workbench? При неостановленной службе MySQL80 ?
	-- будет в обоих случаях.

	* что делать с full.sql ?
	-- вместо него используется import.sql, или любой другой текстовый файл.

	* имя таблицы в hibernate.cfg.xml мы указали, но пока неясно, что с этим именем делать.
	- Вроде бы ответ есть тут: http://www.h2database.com/html/features.html, но читать всё мы не стали, а из того, что прочли, однозначный ответ не получили.
	- Хибер-т умеет читать данные из таблицы, ориентируясь только на тип данных (в нашем случае это тип Product).
	-- Сравнивая полученную информацию, можно сделать вывод, что имя для таблицы указывать необходимо в случаях, когда таблиц несколько и/или когда с таблиц(ей/ами) работют несколько процессов.

	* в import.sql тип IDENTITY можно/нужно ставить ? И имеет ли это значение отношение к @GeneratedValue(strategy = GenerationType.IDENTITY) ?
	-	У преподавателя вместо IDENTITY используется bigserial (это тип PostgreSQL), а в hibernate.cfg.xml есть настройка MODE=PostgreSQL. В общем-то, это не ответ на вопрос, но определённо, его часть. Также частью ответ может быть факт: IDENTITY играет в h2 ту же роль, что и bigserial в PostgreSQL.
	- Хиберт не смущаясь считывает тип IDENTITY.

	* Непонятно, какие значения указывать в hibernate.cfg.xml в качестве логина и пароля для доступа к БД ?
	- В документации к h2 логин sa используется в одном из примеров (http://www.h2database.com/html/advanced.html?highlight=password&search=password#firstFound). Пока это всё, что удлось нарыть.
	- Фактически пустой пароль нормально проходит фейс-контроль.
	- Теоретически, логин и пароль задаются для базы (или таблицы? или для подключения?), а не для доступа к приложению H2 Database. В общем, странный логин sa и пустой пароль годятся.

	* Дарья считает, что в import.xml запросы нужно писать в строку, а не в столбик. Иначе работать не будет. Проверить, так ли это ?
	- Вообще-то у преподавателя неспроста есть метод, который вытягивает в строку содержимое файла full.sql.
	- Символы табуляции хибер-т нормально переваривает.